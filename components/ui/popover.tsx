import { View, Text } from 'react-native';

// Radix UI component - stubbed for React Native compatibility
export const Component = ({ children, ...props }: any) => (
  <View {...props}>{children}</View>
);

export default Component;
